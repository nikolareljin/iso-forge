# Local logs/temp
.deps_install.log
.tmp_config.json
.mockbin/
test_downloads/
# Locally downloaded or user-built terminal image preview binary.
image-view/
ventoy/
!assets/
!assets/ventoy/
!assets/ventoy/**
dist/

# Packaging artifacts
*.deb
*.dsc
*.changes
*.buildinfo
*.rpm
*.srpm
*.tar.gz
*.tar.xz
*.tar.zst
*.log
debian/.debhelper/
debian/debhelper-build-stamp
debian/files
debian/*.substvars
debian/*.log

# OS/editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

/ignore/

# Local notes
TODO.txt

# Ignore Claude memory files outside the repo root
CLAUDE.md
!/CLAUDE.md

# Generated by tools/gen-brew-formula.sh before publishing; never hand-maintained.
packaging/homebrew/isoforge.rb

# rpmbuild tree created by tools/build-rpm.sh
packaging/rpm/build/

# Local recipe overrides, merged over the tracked recipe by ./forge
recipes/*.local.yml
recipes/*.local.yaml

# Default scratch space for ./forge, when it is pointed inside the repo
build/isoforge/
